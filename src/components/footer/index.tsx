import React from 'react';
import { Layout } from 'antd';

const { Footer } = Layout;

const LayoutFooter = () => (
  <Footer style={{ textAlign: 'center' }}>
    Made by Mohammed in React for <a href='https://elinvar.de/'>Elinvar</a>
  </Footer>
);

export default LayoutFooter;
